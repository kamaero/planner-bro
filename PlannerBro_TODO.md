# PlannerBro TODO (общий список)

## P0 (ближайшие релизы)

1. 🚧 Роли и права 2.0 (тонкая модель) — в работе
   - ✅ Отдельные права на уровне пользователя: `delete`, `import`, `team`, `bulk`.
   - ✅ Права подключены в backend для ключевых действий (удаление/импорт/массовые операции/управление командой).
   - ✅ UI «Команда»: редактирование прав и роли сотрудника.
   - ❌ Единая матрица прав для mobile/PWA + скрытие/адаптация всех экранов под права.

2. ✅ История изменений задачи (audit log) — реализовано
   - TaskEvent model + `/tasks/{id}/events` endpoint.
   - Отображается в TaskDrawer (лента событий по задаче).
   - Фиксируются: статус, прогресс, исполнитель, описание, приоритет.

3. 🚧 Массовые операции по задачам (bulk edit 2.0) — частично
   - ✅ Фронтенд: bulk смена статуса + bulk удаление (ProjectDetail.tsx).
   - ✅ Фронтенд: чекбоксы + «Выделить видимые».
   - ✅ Бэкенд: есть `/projects/{project_id}/tasks/bulk` endpoint.
   - 🚧 Массовое назначение исполнителя (multi-assignee), расширенные операции по приоритету/тегам.
   - ❌ Перенос задач между проектами.

4. ✅ Основание запуска проекта — реализовано
   - Поле `launch_basis_text` (номер приказа/протокола) в модели и форме создания.
   - Привязка файла-основания (`launch_basis_file_id` → ProjectFile).
   - Отображение в ProjectDetail рядом с прогресс-баром + кнопка скачивания.

5. ✅ Контроль СКИ — реализовано
   - Чекбокс «Контроль СКИ» в форме создания/редактирования проекта и задачи.
   - При включении приоритет принудительно становится `critical`.
   - Хранится в БД (`control_ski: Boolean`) на уровне проектов и задач.

6. 🚧 Шаблоны проектов и чек-листов — частично
   - ✅ 6 шаблонов при создании: blank, «Запуск проекта», «Сопровождение», «Закупка», «Внедрение», «Интеграция».
   - ✅ DoD-чеклист (4 пункта) в каждом проекте по умолчанию.
   - ❌ Шаблоны чек-листов задач.

7. Расписание/календарь загрузки команды
   - Перегруз/окна/конфликты дедлайнов.
   - Фильтры по роли/команде/проекту.

8. 🚧 Мобильный MVP — частично (Flutter)
   - ✅ Авторизация (Google OAuth + refresh токен в secure storage).
   - ✅ Список проектов и задач.
   - ✅ Canvas-based mini Gantt (задачи с датами).
   - ✅ API-клиент с auto-refresh и logout при 401.
   - ✅ Push-уведомления в мобильном (FCM-токен регистрируется/обновляется/снимается на logout).
   - ✅ Быстрые апдейты статуса (уведомления + «Мои задачи»).
   - ❌ PWA-версия веб-приложения.
   - ❌ Полноценный офлайн-режим (кеш + очередь синхронизации).

9. 🔄 AI-помощник (Project Manager) — в работе
   - ❌ Обнаружение застрявших задач + AI-совет (Celery daily @ 09:00).
   - ❌ Еженедельный AI-дайджест по проектам (Celery Monday @ 08:00).
   - ❌ On-demand анализ проекта в ProjectDetail.
   - ❌ Push + email оповещения от AI PM.
   - Инфраструктура готова: OpenRouter, Celery Beat, FCM, SMTP — всё настроено.

10. Excalidraw-встраивание в карточку проекта
    - Доска брейншторма + привязка заметок/решений к задачам.

## P1 (устойчивость и эксплуатация)

11. ✅ Эксплуатация и безопасность (2026-02-25)
   - scripts/backup-postgres.sh — pg_dump + gzip + ротация 7 дней.
     Для cron на VPS: `0 3 * * * bash /opt/planner-bro/scripts/backup-postgres.sh --local >> /var/log/planner-backup.log 2>&1`
   - GET /health проверяет DB (SELECT 1) и Redis (PING); возвращает "ok"/"degraded" + ws_connections.
   - docker-compose.prod.yml: healthcheck для backend (python urllib); celery_worker/beat ждут service_healthy.
   - Структурированный logging.basicConfig (WARNING) в main.py.
   - Остаток: настроить cron на VPS, алерты (Sentry/Grafana) — отдельная задача.

12. ✅ Нотификации — единый контракт событий (2026-02-25)
   - backend/app/services/events.py — единый источник event-констант.
   - frontend/src/api/events.ts — зеркало констант для TS (WS_EVENTS).
   - notification_service.py, ai_ingestion.py, useWebSocket.ts — используют константы.
   - Остаток: синхронизация read/seen, снижение шума уведомлений.

13. ✅ Realtime устойчивость (2026-02-25)
   - websocket_manager: _last_ping per connection, record_ping(), cleanup_stale(90s).
   - main.py: фоновый цикл cleanup каждые 60 с, логирует удалённые соединения.
   - /health возвращает ws_connections count.
   - Остаток: метрики доставки событий (Prometheus/Grafana).

14. ✅ UX/perf для больших проектов (2026-02-25)
   - Code splitting (vite.config.ts manualChunks): app-чанк 851 KB → 135 KB.
     Vendor-чанки: react, radix, recharts, gantt, query — кешируются независимо.
   - Виртуализация (@tanstack/react-virtual v3): при > 40 задач включается
     virtual scroll (65vh контейнер, estimateSize=110px, overscan=5).
   - Gantt лимит 150 задач с amber-предупреждением если превышено.

15. ✅ Напоминания команды 2.0 + Check-in workflow (2026-02-26)
   - Добавлено поле сотрудника `work_email` (будни: рабочая почта, выходные: личная).
   - Email-напоминания получили deep-link на конкретную задачу в PlannerBro.
   - В задаче реализован check-in без смены статуса:
     `summary`, `blockers`, `next_check_in_due_at`, флаг `need_manager_help`.
   - Напоминания переведены на просрочку `next_check_in_due_at` + отдельное правило
     для `control_ski=true` (ежедневно за 5 дней до дедлайна).
   - Добавлены email-уведомления при назначении в проект/задачу.
   - Добавлен daily audit: отчёт руководителю о проектах/задачах без менеджера/админа.

16. ✅ Сайдбар: команда + монитор SMTP-активности (2026-02-26)
   - Нижний блок сайдбара переработан: список всей команды после текущего логина.
   - Онлайн/оффлайн индикация по классике мессенджеров: зелёная/красная точка.
   - Добавлен backend-журнал `email_dispatch_logs` (`sent`/`failed`/`skipped`).
   - Добавлен API `GET /notifications/activity/email` и UI-блок «Активность системы».

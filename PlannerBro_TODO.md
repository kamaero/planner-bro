# PlannerBro TODO (общий список)

Обновлено: 2026-03-24

## Что уже сделано (фиксируем факт)

1. ✅ Дашборд и компоновка
   - Плотная сетка виджетов без лишних отступов.
   - Виджет «Срочные задачи» (без выбора проекта, отдельный inbox).
   - Виджет «Мои задачи» с сортировкой по срочности и дедлайну.
   - Виджет «Мудрость дня» в Matrix-style, клик для обновления, авто-рандом каждые 15 минут.
   - Виджет «Активность системы» в terminal-style + модальное окно «Открыть лог».

2. ✅ Статусы и модель задач
   - Добавлены статусы: `ТЗ` и `Тестирование`.
   - Быстрые апдейты статуса из мобильного и из уведомлений.
   - История изменений задачи (audit log: события по задаче).

3. ✅ Права и ограничения
   - Базовый RBAC усилен: отдельные права `delete/import/team/bulk`.
   - UI управления ролями и правами в разделе команды.
   - Ограничения на чувствительные действия заведены на backend-проверки.

4. ✅ Уведомления (web + mobile)
   - Единый контракт событий (`events.py` / `events.ts`).
   - Read/seen синхронизация между каналами.
   - Фильтры по типам уведомлений + счётчики.
   - Mixed feed: непрочитанные в приоритете.
   - Попап непрозрачный, читаемый; исправлены позиции колокольчика.

5. ✅ Мобильное приложение (Flutter MVP+)
   - Авторизация + refresh token secure storage.
   - Разделы: Dashboard, Projects, My Tasks, Notifications, Analytics, Settings.
   - Создание проектов и задач.
   - Редактирование задач (статус, прогресс, комментарий/причина, дедлайн, исполнитель).
   - Deep-link из уведомлений в проект/задачу.
   - Push-цепочка FCM: регистрация/обновление/cleanup токенов.
   - Мини-Gantt и аналитика в мобильном.

6. ✅ Техбаза и эксплуатация
   - Healthcheck DB/Redis/WS + cleanup stale websocket.
   - Резервные копии PostgreSQL (скрипт + ротация).
   - Оптимизация frontend bundle + виртуализация длинных списков.
   - Check-in workflow, напоминания и контроль СКИ.

7. ✅ Дайджесты и рассылки (P0 — закрыто 2026-03-24)
   - Ролевые HTML-дайджесты: директор / руководитель / исполнитель.
   - Единый шаблон писем: KPI-блоки, прогресс-бары, приоритет-чипы.
   - Кнопки быстрых действий в письмах: «Взять в работу», «Check-in», «Эскалировать» — подписаны JWT (7 дней).
   - Action endpoint без Bearer-auth для браузерного перехода.
   - Антиспам: дедупликация по получателю+задаче (24 ч), MD5-fingerprint для management audit.
   - Email test-mode: все письма → один адрес, префикс [TEST].
   - Контроль доставки: статистика по источникам (sent/failed/skipped, error-rate) в UI настроек и в боковой панели.

8. ✅ Надёжность рассылок (P0 — закрыто 2026-03-24)
   - Celery `acks_late=True`, `reject_on_worker_lost=True`, `prefetch_multiplier=1`.
   - Redis SET NX locks на всех Celery-чекерах — нет дублей при рестартах beat.
   - Расширенные метрики доставки (источник, error-rate, последняя ошибка).

9. ✅ Роли и права 2.0 (P1.7 — закрыто 2026-03-24)
   - Единый `permission_service.py`: единая матрица для web/mobile/API.
   - `GET /users/me/permissions` — фронт кэширует 5 минут (TanStack Query).
   - Скрытие/блокировка кнопок по правам во всех ключевых экранах.
   - Аудит отказов доступа → `SystemActivityLog` source=`access_control`.
   - Хук `useMyPermissions()` + fallback на безопасные дефолты.

10. ✅ Bulk edit 2.0 (P1.8 — закрыто 2026-03-24)
    - Уже было: статус, исполнитель, приоритет, удаление.
    - Добавлено: сдвиг дедлайна (±N дней с обязательной причиной, пишет DeadlineChange + событие в историю).
    - Добавлено: перенос задач в другой проект (проверка менеджерских прав в целевом проекте; подзадачи с чужим родителем открепляются).

11. ✅ AI Project Manager (P1.9 — закрыто 2026-03-24)
    - On-demand анализ: кнопка «AI Анализ» в шапке проекта → модалка с 4 блоками: оценка / риски / нагрузка / рекомендации 🔴🟡🟢.
    - Ночной механический скан (08:00 UTC, без LLM): флагует проекты с ≥2 просрочками или ≥2 зависшими, шлёт Telegram только при наличии проблем.
    - Еженедельный AI-дайджест (каждый понедельник 08:00 UTC): DeepSeek по каждому активному проекту → серия сообщений в Telegram.
    - Использует существующую AI-инфраструктуру: DeepSeek / OpenRouter fallback.

---

## P1 (в работе / следом)

12. ❌ Telegram UX (отложено)
    - Ролевые дайджесты в Telegram.
    - Компактный и полный режимы сообщений.
    - Инлайн-кнопки быстрых действий.

---

## P2 (backlog)

13. ❌ Календарь загрузки команды
    - Конфликты дедлайнов, окна перегруза.
    - Срезы по ролям/командам/проектам.

14. ❌ PWA-версия
    - Установка как приложение.
    - Push/web-notifications parity.

15. ❌ Полноценный офлайн-режим (mobile)
    - Локальный кеш + очередь синхронизации.
    - Политики merge/conflict resolution.

16. ❌ Excalidraw в карточке проекта
    - Доска решений и связка с задачами.

---

## Критерии готовности ближайшего релиза

1. Все ролевые дайджесты отправляются корректно (email + Telegram) минимум 7 дней без ручных правок.
2. HTML-отчёты выглядят одинаково в ключевых клиентах (Gmail, Outlook, web preview).
3. Быстрые действия из отчётов безопасны и проходят по правам.
4. AI-анализ стабильно возвращает результат на реальных проектах.
5. Антиспам и тест-режим подтверждены вручную на реальных рассылках.
